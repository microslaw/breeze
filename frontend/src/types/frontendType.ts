export enum FrontendTypeEnum {
  Plaintext = "plaintext",
  Image = "image",
  HtmlDiv = "html_div",
  HtmlWebsite = "html_website",
  Json = "json",
  Integer = "integer",
  Decimal = "decimal",
  // Used only if there was an error during processing (ont a real frontend type)
  ERROR = "error",
}
